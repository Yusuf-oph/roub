# Déploie le contenu de app/ sur GitHub Pages à chaque push sur main.
name: pages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      # majors tenus à jour : les v4/v5/v3/v4 précédentes tournaient en node20,
      # que GitHub signale comme périmé ; ces quatre-là sont en node24
      - uses: actions/checkout@v7
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: app
      - id: deployment
        uses: actions/deploy-pages@v5
        with:
          # le site pèse ~130 Mo (823 mp3 embarqués) : le délai par défaut de
          # 10 minutes ne suffit plus quand Pages est lent, l'action abandonne
          # sur « Timeout reached, aborting! » alors que rien n'est cassé
          timeout: 1800000
