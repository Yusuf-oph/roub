# archives de partage locales
quran-hifz-*.zip
roub-*.zip

# caches regénérables du pipeline
tools/cache/
__pycache__/

# retours privés (les avis des amis ne vont pas sur un dépôt public)
feedback-recus/
app/data/feedback.json

# pièces de travail interne (licences archivées, échantillons, arbitrages)
arbitrage/

# pièces mises de côté (PoC terminés, essais) : gardées, pas publiées
archive/

# outillage local
.claude/
